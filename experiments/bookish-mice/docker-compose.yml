version: '3.7'
services:
  nginx:
    image: nginx:stable
    restart: "no"
    volumes:
      - ./public:/usr/share/nginx/html:ro
    ports:
      - 80
    networks:
      default:
      buoy:
        aliases:
          - bookish-mice

networks:
  buoy:
    external:
      name: buoy
